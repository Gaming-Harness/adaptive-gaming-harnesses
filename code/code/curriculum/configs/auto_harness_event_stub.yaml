# Event Auto — aligned with Auto K=1 temp=0 (stub).
seed: 0
logdir: curriculum/outputs/auto_harness_event_stub
n_rounds: 4
n_eval_episodes: 1
eval_seeds: [101]
holdout_seeds: [909]
accept_eps: 0.01
start: strong
preserve_harness_body: false
proposer_mode: bank

backend: stub
vla_mode: stub
vla_temperature: 0.0
vla_do_sample: false
max_steps: 36
action_chunk_len: 4
checkpoint_every: 4

probe_on_stall: true
probe_on_fail: true
warmup_probes: 0
stall_steps: 8
routing_on_stall: observe
routing_on_fail: recover
recover_max_retries: 2
observe_budget: 8
probe_action_pool: [forward, attack, forward_attack, turn_left, look_up, look_down]
