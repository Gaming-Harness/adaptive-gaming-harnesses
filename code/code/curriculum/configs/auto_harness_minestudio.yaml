# Live MineStudio harness search (sandbox token required)
#   stub VLA:  auto_harness_minestudio.yaml / *_smoke.yaml
#   base Qwen hf: auto_harness_minestudio_hf.yaml

seed: 0
logdir: curriculum/outputs/auto_harness_minestudio
n_rounds: 6
n_eval_episodes: 2
accept_eps: 0.01
start: degraded
backend: minestudio
vla_mode: stub
ticks_per_action: 4
# Required for live create_env (empty task previously failed with remote minerl)
task_config: /path/to/openha_tasks/0000_mine_block_oak_log.json
img_save_dir: curriculum/outputs/harness_sandbox_images
success_reward_thresh: 0.5
soft_rollback: true
